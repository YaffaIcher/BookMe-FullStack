{
  "Logging": {
    "LogLevel": {
      "Default": "Information", // Default logging level for the application
      "Microsoft.AspNetCore": "Warning" // Logging level for ASP.NET Core-related logs
    }
  },
  "AllowedHosts": "*", // Allows requests from any host
  "ConnectionStrings": {
    "sql": "Server=PC-77114\\MSSQLSERVER01;Database=BookMe;Trusted_Connection=SSPI;Encrypt=false;TrustServerCertificate=true;" // Connection string for the SQL Server database
  }
}
